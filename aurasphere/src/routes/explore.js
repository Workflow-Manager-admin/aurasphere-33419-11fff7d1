const express = require('express');
const exploreController = require('../controllers/explore');

const router = express.Router();

// PUBLIC_INTERFACE
// Define explore page routes

module.exports = router;
