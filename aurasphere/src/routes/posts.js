const express = require('express');
const postsController = require('../controllers/posts');

const router = express.Router();

// PUBLIC_INTERFACE
// Define post-related routes (create post, get posts, etc.)

module.exports = router;
